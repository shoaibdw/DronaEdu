package com.drona.drona.models;


/**
 * The `BookingType` enum represents the types of course bookings available.
 * It defines two possible values: "GROUP" and "ONEONONE."
 */
public enum BookingType {
    GROUP, // Represents a group course booking.
    ONEONONE; // Represents a one-on-one (individual) course booking.
}
